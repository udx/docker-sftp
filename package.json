{
  "name": "docker-sftp",
  "version": "0.14.0",
  "description": "SSH tunnels to Kubernetes containers",
  "main": "bin/server.js",
  "scripts": {
    "dev-start": "worker service restart rabbit-ssh-server",
    "start": "worker service restart sshd && worker service restart rabbit-ssh-server",
    "test": "node --check lib/controller.keys.js && node --check lib/firebase.consume.js && node --check lib/server.js && node --check lib/utility.js && node -e \"const app = require('firebase-admin/app'); const database = require('firebase-admin/database'); if (!app.initializeApp || !app.cert || !database.getDatabase) process.exit(1)\" && bash -n bin/controller.ssh.entrypoint.sh && bash -n bin/setup-kubernetes.sh"
  },
  "bin": {
    "controller.keys": "lib/controller.keys.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/udx/docker-sftp.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/udx/docker-sftp/issues"
  },
  "homepage": "https://github.com/udx/docker-sftp#readme",
  "dependencies": {
    "async": "^3.2.6",
    "axios": "^1.18.1",
    "debug": "^4.4.3",
    "dot-object": "^2.1.5",
    "express": "^5.2.1",
    "firebase-admin": "^14.1.0",
    "lodash": "^4.18.1",
    "md5": "^2.3.0",
    "mustache": "^4.2.0"
  }
}
